# 禹合 ERP 系統健康報告(2026-08-08)

盤點對象:`禹合真正營運版_V5_3_MASTER_0614`(Google 試算表)+ `禹合制所 App`(Apps Script)。
以下依優先級列出實際查證到的問題與修法。**動手前先備份**(整份試算表「建立副本」)。

---

## 🔴 最高優先

### 1. ERP_03_工作安排:忠泰湛同案有兩組案件ID,重複排程
- 查證:忠泰湛的任務同時掛在 `C260709-02`(8 列)與 `C260711-01`(11 列),另有 5 列**案件ID 空白**。
- 影響:Telegram 每日戰報同一件事重複提醒(戰報畫面上「提案 Final 準備」「平配+簡報提案」同天多條就是這個)。
- 修法:
  1. 先確認 App 以哪組 ID 為準(看 ERP_案件 主檔忠泰湛登記的 ID)。
  2. 把另一組 ID 的任務列**搬到封存分頁**(不要直接刪,總表可能有公式引用)。
  3. 5 列空白 ID 補上正確案件ID。
- 另查到:全表 109 列任務中 **73 列案件ID 空白**(台北華府、鉅力高宇D等)。不影響推播文字,但影響之後按案件彙總,建議陸續補。

### 2. 試算表時區是 America/Los_Angeles(不是台北)
- 影響:Apps Script 若用試算表時區,「今天」會比台灣慢 15–16 小時,逾期天數與每日戰報的日期切點都可能偏一天。
- 修法:**不能直接切**。先看 Apps Script 程式:
  - 若程式內已寫死 `Session.getScriptTimeZone()` 或 `Utilities.formatDate(..., 'Asia/Taipei', ...)` → 可安全把試算表時區改為台北(檔案→設定→時區)。
  - 若程式用 `SpreadsheetApp.getActive().getSpreadsheetTimeZone()` → 改時區前要同步測試推播。
  - ⚠️ 我目前讀不到 Apps Script 原始碼(工具不支援)。請把「禹合制所 App」的程式碼複製貼給我,或匯出放進這個 repo,我幫你逐行確認後給安全改法。

### 3. 逾期任務 63 項,多半是「做完但狀態沒改」
- 查證:ERP_03 共 109 列,102 列狀態=待處理、只有 7 列=已完成;其中 63 列日期已過。
- 影響:Telegram 戰報每天轟炸「已過期 N 天」,狼來了之後大家會無視真正的逾期。
- 修法(流程面,比技術面重要):
  - 每天收到戰報後,把實際完成的在 ERP_03 把狀態改成「已完成」(或請 App 加一個 Telegram 回覆「完成 #編號」自動改狀態的指令——這個可以之後做)。
  - 已過期且確定不做的,狀態改「取消」。

---

## 🟡 高優先

### 4. 00_老闆總表 有 2 格 #REF!
- 位置:「需追蹤收尾」「逾期提醒」兩格(公司總覽區塊右側)。
- 原因:公式引用的舊分頁(`12_收尾提醒清單` 等)已改名/刪除,現行分頁叫 `12_缺失待辦`。
- 修法:點開兩格看原公式,把來源改指到 `12_缺失待辦` 對應欄位即可。

### 5. 老闆總表引用「封存頁」資料,且部分數字為手動值
- `zz_02_客戶收款明細_封存` 等仍被總表公式引用 → 封存頁**不能刪不能改名**(導覽頁已標注)。
- 老闆總表的彙總數字與 01_案件總控 已對不上(例:總表待收工程款 3,639,800,總控現算為 9,635,750 口徑不同/未更新)。
- 修法:選一個「唯一真相來源」——建議一律由 `01_案件總控` 現算,總表改公式引用總控,不再手填。

### 6. 日期格式混用(文字 vs 真日期)
- 實錘:19_現金流儀表板 豐邑窗簾/淋浴門兩列日期顯示成序號 `46214`(=2026/7/11);其他列有的是文字有的是日期。
- 影響:排序、逾期計算、行事曆同步都可能踩雷。
- 修法:先不要整批轉(導覽頁也這麼囑咐)。等 App 程式碼確認讀取方式後,統一成 `yyyy/mm/dd` 日期格式。

### 7. 老闆總表「本週請款提醒」有重複列
- 合新合心尾款 122,000(9/30)出現兩次。
- 修法:刪一列即可(這區是人工區,安全)。

---

## ✅ 沒問題/已很好的部分

- 01_案件總控 資料新鮮(8/5 忠泰湛簽約、8/6 提平配都已入表),9 案金額完整。
- 各案損益表結構清楚,毛利率有算(豐邑 44%、高宇 39%、合新 45%)。
- 27_行事曆同步、AI 工地照片、工地日誌都在運作。
- 00_營運導覽 這頁寫得很好,本報告即以它為起點查證。

---

## 📊 戰情室看板(v2)使用方式

`禹合戰情室看板.html` 已內嵌 2026-08-08 快照(9 案現金流 + 109 項任務)。

要升級成**即時看板**:
1. 開 MASTER 試算表 → 檔案 → 共用 → **發佈到網路**。
2. 分頁選 `01_案件總控`,格式選 **CSV**,複製網址。
3. 再發佈 `ERP_03_工作安排` 的 CSV,複製網址。
4. 打開 `禹合戰情室看板.html`,把兩個網址貼進檔案開頭的 `LIVE = { ctrl:'…', tasks:'…' }`。
5. 之後表一改,看板重新整理就是最新的。

> 注意:發佈到網路=知道網址的人都能看到那兩個分頁的內容。若在意,改用「僅限連結共用+gviz」方式,跟我說我再調。

---

## 下一步建議順序

1. **你貼 Apps Script 程式碼給我** → 我確認時區與 Workflow 讀取範圍(解 #1 #2 的前提)。
2. 修老闆總表 2 格 #REF! 與重複列(#4 #7,十分鐘內的事)。
3. 發佈 CSV,看板上線活資料(#上方步驟)。
4. 建立「每日戰報後回寫狀態」的習慣或指令(#3)。
5. 之後再做:日期格式統一(#6)、總表單一真相來源(#5)、案件ID 補齊。
