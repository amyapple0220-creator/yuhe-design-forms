# 合雄凰璽 B1／7F 現況平面 CAD

## ⚠ 先看這段

**牆的軸線位置目前仍是推估值**，不是丈量成果。**校正前不可用於施工、放樣或估價。**

2026/08/20 已收到 8/15 的現場丈量標註圖（照片，5 頁）。判讀結果分兩類：

- **項目級尺寸已確認並寫進圖**：窗寬／窗高／窗台高、門寬／門高、樓板結構高 CH 291.6、
  樑下淨高 H 262.3。這些與牆的位置無關，判讀信心高。
- **牆的位置尚未定案**：另有 28 筆讀得出來的數字，但**無法確定各自量的是哪一道牆**。
  照片是斜拍且有摺痕，透視校正後仍殘留 ±1.5°，從影像反推比例尺經測試無鑑別力，
  因此**沒有硬猜**。缺的是兩條開間尺寸鏈（橫向、縱向）。

判讀明細與待確認項目見 `../合雄凰璽B1-7F_丈量標註判讀表.html`。

圖框左上角會蓋一行警語，校正完請把產生器裡的 `NOTE_STAMP` 改成「依現場丈量繪製」。

## 檔案

| 檔案 | 用途 |
|---|---|
| `合雄凰璽B1-7F_現況平面_產生器.py` | 主檔。改數字、重跑，就出新圖 |
| `合雄凰璽B1-7F_現況平面.dxf` | Big5／ANSI_950，給中文版 AutoCAD |
| `合雄凰璽B1-7F_現況平面_UTF8.dxf` | UTF-8，給 QCAD／LibreCAD／SketchUp |
| `合雄凰璽B1-7F_現況平面.svg` | 瀏覽器直接開，手機也能看 |

DXF 為 R12（AC1009），比例 1:50，圖框 A3，單位 mm。
**中文變亂碼就改開 `_UTF8.dxf`**，反之亦然。

## 業主標註體系（判讀自丈量圖）

| 寫法 | 意思 | 例 |
|---|---|---|
| `W###` | 窗寬 | W238.4 |
| `⊥ 上／下` | 上＝窗高，下＝窗台高 | 191.8 ／ 39+4.5 |
| `DW##` / `DH###` | 門寬 / 門高 | DW90 / DH215 |
| `H:###` | 樑下淨高 | H:262.3 |
| `CH:###` | 樓板結構高 | CH:291.6 |
| `a+b+c` | 尺寸鏈（含牆厚） | 50.3+18.5+314.1+20 |

**CH 291.6 − 樑下 262.3 ＝ 樑深約 29.3**，是天花設計的硬上限；
業主要求全室地坪齊平，地坪墊高會再吃掉淨高，兩者要一起算。

## 怎麼把丈量數字填進去

打開 `合雄凰璽B1-7F_現況平面_產生器.py`，只改最上面「丈量數據區」那一段：

1. **`GRID_X` / `GRID_Y`** —— 各道牆的軸線位置（mm，從左下角外牆外緣起算，累加）
   目前是：
   - `GRID_X = [0, 1700, 5400, 6800, 10400, 13200]`
     前陽台 ｜ 女兒房1 ｜ 走道 ｜ 廚房 ｜ 後陽台
   - `GRID_Y = [0, 2600, 4400, 6300, 7400, 10400]`
     主浴.房3 ｜ 主臥 ｜ 客浴 ｜ 廚房.後陽台
2. **`T_EXT` / `T_INT` / `COL`** —— 外牆厚、隔間厚、柱寬
3. **`OPENINGS`** —— 門窗開口：`(種類, 牆向, 軸線, 起, 迄, 牆厚, 門樞側, 開啟方向)`
   - 種類：`"D"` 門、`"W"` 窗、`"O"` 無門開口
   - 牆向：`"H"` 水平牆（沿 X）、`"V"` 垂直牆（沿 Y）
   - 門樞側：`"s"` 在起點、`"e"` 在迄點；開啟方向 `+1` / `-1`
4. **`ROOMS`** —— 房間範圍與名稱
5. **`FIXTURES`** —— 現況固定設備（浴缸、馬桶、流理台…），供拆除範圍判斷

存檔後執行：

```bash
python3 合雄凰璽B1-7F_現況平面_產生器.py
```

會重新輸出三個檔，並在終端機印出**各室面積對帳表**：

- **軸線面積**（含牆）—— 對應權狀實坪的算法，拿來跟「室內約 40 坪」對帳
- **淨地坪**（扣半牆）—— 實際可用地坪，拿來排傢俱與算地板數量

兩者差距約 3 坪是正常的（牆體佔用）。若軸線合計與 40 坪差超過 1 坪，
代表 `GRID_X` / `GRID_Y` 有一段量錯或漏掉，回去對丈量草圖。

## 圖層

| 圖層 | 內容 |
|---|---|
| `00-圖框` | A3 圖框與標題欄 |
| `01-結構牆` | RC 外牆 |
| `02-隔間牆` | 室內隔間 |
| `03-柱` | 結構柱（實心） |
| `04-窗` | 窗 |
| `05-門` | 門片與開啟弧線 |
| `06-陽台雨遮` | 陽台、雨遮地坪示意 |
| `07-固定設備` | 現況衛浴、廚具 |
| `08-尺寸` | 尺寸線 |
| `09-文字` | 房名、面積、標題欄 |
| `10-註記` | 警語 |

## 現場還要補量的項目

以下是這份底圖沒有、但施工圖與估價一定會用到的，**複勘時要補**：

- **橫向與縱向的完整開間尺寸鏈**（最優先，缺這個 CAD 無法定案）
- **樑位平面**：目前只有兩個高度值，沒有樑的位置與寬度
- 天花板淨高（現況天花為木作包覆，拆除後才是真實高度）
- 管道間實際位置與尺寸、糞管落點
- 配電箱位置、迴路數、總電量
- 窗框外框尺寸（是否更換）
- 各房現況地坪高低差（需求表要求全室齊平）
- 大門尺寸（業主指定保留不換，須量準）

## 已知待確認

- 建商圖標示為 3 房 2 廳 2 衛，與需求表相符；但 12 年屋前手可能已改隔間，**以現場為準**
- 廚房現況為 L 型（照片可見），需求為 ㄇ字型＋吧台，屬變更範圍
- 主浴現況已有浴缸＋淋浴拉門，但未做乾濕分離；客浴需增設浴缸
